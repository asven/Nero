@model Nero.Model.EmailUser

<div id="footer">
    <div class="container 75%">

        <header class="major last">
            <h2>Questions or comments?</h2>
        </header>

        <p>
            If you would like more information on what we do, send us a message and we will get back to you as soon as possile.
        </p>

        @using(Html.BeginForm("Index", "Email"))
        {
            <div class="row">
                <div class="6u 12u(mobilep)">
                    @Html.TextBoxFor(x => x.Name, new { placeholder = "Name" })
                </div>
                <div class="6u 12u(mobilep)">
                    @Html.TextBoxFor(x => x.Name, new { placeholder = "Email" })
                </div>
            </div>
            <div class="row">
                <div class="12u">
                    @Html.TextAreaFor(x => x.Name, new { rows = "6", placeholder = "Message" })
                </div>
            </div>
            <div class="row">
                <div class="12u">
                    <ul class="actions">
                        <li><input type="submit" value="Send Message" /></li>
                    </ul>
                </div>
            </div>
        }

        <ul class="icons">
            
        </ul>

        <ul class="copyright">
            <li>&copy; Nero Engineering. All rights reserved.</li>
            <li>Design: <a href="http://html5up.net">HTML5 UP</a></li>
        </ul>

    </div>
</div>